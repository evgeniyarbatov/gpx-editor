# GPX Editor

Split large GPX file into smaller segments compatible with Polar and Garmin.

Site: https://gpx-editor.gritcuriosityandperseverance.org

## How to run

- Local dev server: `make run` (runs `npm run dev` in `site/`).
- Deploy: `make deploy` (builds the site and applies Terraform).
